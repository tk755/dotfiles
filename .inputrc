# CLI keyboard mapping

# make Ctrl+Backspace delete words backward
"\C-h": backward-kill-word # fix for modern terminals: WSL, GNOME, Konsole, xterm, etc.
"\C-?": backward-kill-word # fix for old terminals: rxvt, urxvt, etc.

# show completion matches immediately (no double-tab needed)
set show-all-if-ambiguous on

# color-coded tab completion
set colored-stats on